# PULSE API — Vercel serverless runtime dependencies
# Total must stay under 250MB unzipped.
# scipy (~150MB) is intentionally ABSENT — this runtime never computes.
# The deployed service is read-only over persisted runs (F2/D13); the
# scipy-only engine runs OFFLINE (scripts/run_50k_prod.py). No numpy
# fallback exists anywhere (D13) — do not add scipy or a fallback here.
# pandas (~60MB) absent — not needed at runtime (persisted/seed data only).
fastapi>=0.110
pydantic>=2.6
numpy>=1.26
psycopg2-binary>=2.9
aiohttp>=3.9
feedparser>=6.0
requests>=2.31
