node_modules/
__pycache__/
*.pyc
.env
*.db
.venv/
dist/
dist-*/
*.xlsx
*.egg-info/
.DS_Store
.next/
.vercel/
.hypothesis/
tsconfig.tsbuildinfo
# Office app lock files
.~lock.*#
~$*

# Vercel env upload helper — never commit
.env.vercel-upload*

# Deploy token for Cowork / sandbox auto-push — never commit
.env.deploy

# Post-deploy placeholder from prior token-upload flow — safe to delete locally
DEPLOY_TOKEN_HIER_EINFUEGEN.txt

# Sandbox / IDE local prefs — not part of the product
.claude/settings.local.json

# Python backup files from one-off migration scripts
*.py.bak.*

# SQLite write-ahead / journal side-files
*.db-journal
*.db-wal
*.db-shm
_RETIRED_2026-06/

# Handover-Cleanup 2026-06-05
.claude/worktrees/
pulse_server.log
*.tsbuildinfo

# Nicht-App-Dateien (Decks, Reports, Audits) — nur lokal/OneDrive
_ARCHIVE/
_BACKUP_pre_v3.7_2026-06-10.tar.gz

# clerk configuration (can include secrets)
/.clerk/

# Handover quarantine 2026-06-11 — never commit, never ship
_NOT_FOR_HANDOVER/
