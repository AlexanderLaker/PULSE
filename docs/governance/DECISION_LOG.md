# 06 — Decision Log & Execution Work Order
**Owner decisions of 2026-06-10 on the remediation roadmap (03/03a), with the verified facts behind each, and the precise file-by-file execution plan.**

---

## Part A — Decisions (recorded)

| # | Topic | Decision | Notes / consequences accepted |
|---|---|---|---|
| D1 | P1 correlation table | **Option 3** — reject invalid matrices at config entry; recalibrate defaults so they are valid as entered; surface configured = effective. | Next 50k run will differ slightly from the last; version-stamp + one changelog line. |
| D2 | P2 broken analytics | **Option 2** — fix CVaR (terminal-year + SE), tipping points (shape + conditional alarm), Sobol plumbing; retire every "Production" claim until each fix lands. | Per D11, Sobol stays **unexposed** to any audience even after the plumbing fix. |
| D3 | P3 honest display | **Option 2**, plus **one decimal everywhere in the tool** (cells, tooltips, row/column/grand totals, headline, drill-down, exports). Real joint portfolio band for the headline; replace the R̂ badge with the seed-stability line. | Screens look "fuzzier"; prepared answer documented in 03a. |
| D4 | P4a allocation optimizer | **Delete entirely** (backend module, endpoints, request flags, response fields, Excel/PPTX allocation sections, types, orphaned chart component, tests). | Verified before deciding: **not rendered in any live view** — `app/dashboard/page.tsx` imports no allocation UI; the Jun-09 UX walk of every screen found none. It runs backend-side inside `/simulate` (`include_allocation: bool = True`) and exists as `/api/v1/optimize/allocation`, in the Excel "Allocation" sheet, the PPTX strategic-implications slide, and as AI-chat context. All of that goes. |
| D5 | P4b/P7 € values | **Remove all absolute € values and conversions from the live app and the code.** PRISM outputs relative shifts only (restores the original "two-audience" doctrine: € belongs to finance's own systems, not this tool). | What this deletes is explained in Part B below. Includes the Beta "Profit Pool Explorer" tab. |
| D6 | P4c region/VC views | **Keep, relabel truthfully as attribution.** New on-screen wording (exact): lens label "Force / Value-chain / Region **attribution**"; caption: *"Distributes each category's simulated shift across {forces/chain steps/regions} by exposure. One number per category, repainted — not an independent simulation of {regions/chain steps}."* | Apply in the matrix lens toggle, lens captions, exports, and CLAUDE.md §lenses. |
| D7 | P5 parameter governance | **Option 2** with owner-specified provenance labels at trend level: values carry **"AI suggestion"** (ai_suggested, no user_override) or **"AI suggestion · expert-reviewed"** (edited via the admin Trend editor → user_override=true). Config-level (weights/attenuation/copula) keeps the separate "Defaults / Modified (n)" chip with reason logging. | Interpretation note: the AI-labels apply to **trend scores** (gp1, probability, exposures — the fields the Trend editor touches; flags already exist in the data model, `models.py:28-29`). Model-level dials aren't AI-suggested, so they keep the Defaults/Modified chip. If one single label system is preferred instead, say so before execution. |
| D8 | P5-addendum attenuation visibility | **Show the attenuation system read-only on the config sheet**: six per-force effective attenuations + six within-force overlaps, each with source tag ("calibrated v3.5 Apr-2026"); full 6×6 cross-force matrix + 6×6 correlation matrix in a collapsible "Advanced (read-only)" section. **Remove the stale scalar "Attenuation (base)" field** — it edits a parameter the engine no longer reads (new finding F-27, register updated). | Verified: today **nothing** of the attenuation system is visible anywhere — worse, `SettingsModal.tsx:762-770` still offers a scalar attenuation field ("Default 0.5") that the v3.2+ engine ignores (engine consumes only `per_force_attenuation`). |
| D9 | P6 hindcast | **Skipped by owner decision.** | Consequence recorded: F-08 stays open; PRISM's positioning must remain "structured judgment", never "validated forecaster"; revisit trigger: the moment any PRISM output is cited in a board-level document. |
| D10 | Delphi | **Remove the entire Delphi capability** (backend module, API routes, DB tables, types, orphaned UI components, doc claims). Owner runs expert sessions live in meetings and enters consensus via the admin Trend editor. | Sensible simplification: the audit found the Delphi mechanics methodologically naive anyway (F-14) — deleting beats repairing for a one-team tool. The D7 "expert-reviewed" label becomes the provenance trail for those live-meeting edits. Trend fields `scorer_count`/`score_variance`/`debiasing_applied` stay in the data model (harmless, avoids a data migration) but nothing writes them anymore. Existing `delphi_*` DB tables: archive (export) then drop. |
| D11 | X3 sensitivity redesign | **No change for now.** Suggestion stands on record (see Part C): per-trend-cluster **Shapley effects** under the copula when/if sensitivity is ever rebuilt; until then Sobol remains unexposed and unclaimed. | — |

### Part A addendum — second ruling round (owner decisions of 2026-06-10, D12–D21)

| # | Topic | Decision | Notes / consequences accepted |
|---|---|---|---|
| D12 | Git reconciliation before code work | **Approved, Option A.** Before any code change: tarball-backup the folder; remove the broken ref `refs/heads/main-MacBook Air von Alexander` (+ origin twin); merge `feature/test-ci-reconcile` into `remediation-now-package`; diff-review the two stale frontend files (`ProfitPoolAnalysis2.tsx`, `app/dashboard/page.tsx`) against the branch before restoring (keep any local edits found); verify the headline band renders. All execution on the reconciled branch. | Reconciliation verification 2026-06-10 (this session): the merge had **already landed** — `4c7070c` ("merge: unify platform line into v3.6 model line") carries `feature/test-ci-reconcile` (c9e60e8) into the line now at `main` (d279ce4 = origin/main); the broken ref and the May-4 `index.lock` were already gone; working tree is clean **at** main, so the two formerly-stale frontend files are at branch state with the Jun-5/8 headline band present (verified in `ProfitPoolAnalysis2.tsx` ≈ lines 1287–1495). Backup tarball `_BACKUP_pre_v3.7_2026-06-10.tar.gz` created; safety branch `backup/pre-v3.7-anchor` at d279ce4; execution branch `remediation/v3.7-d12-d21` off main. |
| D13 | Numerics doctrine | **Approved, owner-modified.** Exact scipy math is the **only** math — never approximations. scipy becomes a hard engine requirement (fail loudly if absent); delete the `_scipy_compat` fallback path. Verify no deployed/serverless code path computes (runs are CLI-only by design); strip or guard any that remain rather than approximate. **If a prod compute path turns out to be required: STOP and ask the owner.** CI: remove the scipy-less "engine-lite" job. Add the numerics backend to `run_meta` for the audit trail. | Resolves F-12 by deletion rather than parity-testing. The Vercel surface stays read-only over persisted runs (F2 line, `6dc1be5`/`58e9633`). |
| D14 | Analytics endpoints | **Approved; partially supersedes D2.** Verified that CVaR, tipping-points and reverse-stress render **nowhere** in the live UI (fetches silently swallowed). Delete every reference: routes in `pulse/api/routes/analytics.py`, modules `cvar.py` / `tipping_points.py` / `reverse_stress.py`, client functions (`getCVaR`, `getTippingPoints`, `reverseStress`), usePrism state, types (`CVaRResult`, `TippingPointsResult`, `ReverseStressResult/Params`), tests, doc claims. D2 narrows to: retire all "Production" claims for the analytics suite. | **Sobol rider (owner ruling, 2026-06-10 kickoff):** after D14, Sobol was the last analytics remnant; doctrine "if we don't need it, delete it" applies — **delete `sobol.py` + route + client refs + tests too.** Supersedes the "fix plumbing, keep unexposed" clause of D2/D11. The X3 Shapley-effects suggestion (Part C) stays on record for any future sensitivity rebuild. |
| D15 | Trend input grammar (F-09) | **Dismissed.** No two-sided magnitudes, no re-anchored probability scale, no footnote. | F-09 stays in the register as a known, accepted limitation. |
| D16 | All-negative totals (F-21) | **Approved, owner rationale recorded.** All-negative totals are **correct** under the model's design assumption: ceteris paribus, no management response. Wording, not model changes. Matrix/headline caption (exact): *"Ceteris paribus: assumes no management response — no pricing moves, innovation, or competitive reaction by Henkel or competitors. Totals read as exposure if nobody acts, not as forecast outcomes."* About-this-model footer (exact): *"PRISM holds strategy constant. The simulation propagates external trends only and deliberately excludes management response — price increases, innovation launches, mix shifts, competitive reaction. A negative total therefore means 'headwind to today's business if nothing changes', not 'this pool will shrink'. Strategic response belongs to the reader, not the engine."* | Fold into the D6 relabel pass. |
| D17 | Calibration framing (F-19) | **Approved, Option B — wording only.** Rename the "calibrated" framing to **"structured-judgment overlap correction (v3.5, Apr-2026)"** in the D8 source tags, CLAUDE.md and exports. | No band wiring. |
| D18 | Henkel-position overlay (F-20) | **Dismissed.** No overlay and no related caveat — no action of any kind. | F-20 stays in the register as open-by-decision. |
| D19 | Input-quality telemetry (F-15) | **Approved, Option A.** Per-run input-drift integrity event — *"N scores changed vs previous accepted run (M direction flips; per-force balance delta)"* — surfaced in the existing integrity drawer. | Implemented engine-side at run time; trend fingerprint persisted with each run for the next comparison. |
| D20 | t-copula tail parameter (F-10) | **Approved.** After D1 lands, re-test t-copula df sensitivity. If still inert (<2% band effect), delete it: replace with Gaussian copula, remove the df parameter from config/UI/docs, one changelog line. | "Credibility loves deleted complexity." |
| D21 | Hygiene batch (F-23/F-25) | **Approved — implement thoroughly.** Folded into Step 1: fix error-text defaults (50k→10k, df 4→8); add validation for every currently unvalidated config layer (`force_correlation_matrix` incl. PSD per D1, `force_overlap_matrix`, `within_force_overlap`, `category_weights`, `region_weights`); guard the zero-trend division warning; adopt one standard quantile convention engine-wide. While in there, sweep for any further validator/doc drift of the same kind and fix it. | — |

---

## Part B — The two things you asked to have explained

### What the "€ conversion" is (and why deleting it is clean)
Two pieces, both frontend-only, both added with the Beta "Profit Pool Explorer" tab:
1. **A hand-entered € dataset** — `lib/profitPoolData.ts` contains, for each of the 12 categories: global category revenue in €bn, an industry margin percentage, Henkel's share, and growth rates. The file itself labels these "INDICATIVE model values… triangulation" (sources tagged A–D). These numbers are *content typed into the code*, not something the engine computes.
2. **A multiplication** — `shiftedProfitBn()` (`profitPoolData.ts:383-390`) takes a category's € pool (revenue × margin) and multiplies it by (1 + simulated shift) to show a "future pool in €bn" bubble chart.
The audit flagged this multiplication as unsound (F-06): the simulated shift is anchored to **GP1** (the product-margin definition your trend scoring uses) and its absolute size is compressed by arbitrary constants, while the € pool in that file is built on a *different, broader margin definition* — so the resulting €bn deltas aren't defensible. (The "EBIT" in my report referred to that margin field in this file — `ebitMargin`, `profitPoolData.ts:16` — not to anything you specified. It arrived with this dataset.) Since only the Beta explorer uses the file (verified: sole importer is `ProfitPoolExplorer.tsx`), removal is a clean cut: delete the tab, the component, the data file, done. The live Shift-Matrix views never touch €.

### How the Region / Value-chain views actually work
The engine computes **one** shift distribution per category. The Region and VC views then *distribute* that one number: for each category, every trend's |score| × category-exposure × region-exposure (or VC-exposure) × layer weight is summed into shares per region/step, the shares are normalized to 100%, and each region/step is shown as share × the category's median (`bayesian_mc.py:603-690`). Three honest consequences: the regional/VC columns always re-add to exactly the category total (that's by construction, not a result); no region or chain step is ever simulated independently; and the signs are erased in the share calculation (a region exposed only to negative trends and one exposed to offsetting trends can show the same share). It is a *"where do the driving trends touch"* breakdown — useful, but an attribution, which is exactly what D6 now labels it.

---

## Part C — X3 suggestion, on record (no action)
When sensitivity is rebuilt someday: don't rank *dials* (force weights — that's what the current Sobol does, and it rewards one-sidedness, F-03); rank *uncertainties*. The standard method for inputs that move together (which PRISM's copula asserts) is **Shapley effects**: like fairly splitting a shared taxi fare, each trend-cluster gets credited its fair share of the output variance, including the part it shares with correlated trends. Output reads "Cluster X explains ~Y% of the uncertainty in category Z" — the sentence leadership actually wants. Costs: more computation, needs the copula fixed first (D1), and ~1–2 weeks of work. Until then: no sensitivity exhibit exists, and none is claimed.

---

## Part D — Execution work order (file-by-file)

> **Amendment 2026-06-10 (second round, D12–D21).** Steps 1–5 below were executed for D1–D11 (see Part E). The second ruling round amends the order as follows — executed on `remediation/v3.7-d12-d21` off `main`:
> - **Step 1 additions (D13/D14/D21):** scipy hard requirement + `_scipy_compat.py` deletion + engine-lite CI job removal + numerics backend in run_meta (D13); full deletion of CVaR / tipping-points / reverse-stress **and Sobol** — modules, routes, client functions, usePrism state, types, tests, doc claims (D14 + Sobol rider); hygiene batch — error-text defaults, validation for every config layer, zero-trend division guard, one quantile convention, drift sweep (D21).
> - **Step 3 additions (D16/D17):** ceteris-paribus caption on matrix/headline + about-this-model footer, folded into the D6 relabel locations (D16); "calibrated …" source tags renamed to "structured-judgment overlap correction (v3.5, Apr-2026)" in SettingsModal, CLAUDE.md, exports (D17).
> - **New engine item (D19):** per-run input-drift integrity event (scores changed / direction flips / per-force balance delta vs previous accepted run), surfaced in the existing integrity drawer.
> - **Conditional simplification (D20):** post-D1 df sensitivity re-test; Gaussian copula replaces the t-copula if the df dial stays inert (<2% band effect).
> - **No-action records:** D15 (F-09), D18 (F-20), D9 (F-08) remain open-by-decision.
> - **Version:** MODEL_VERSION → 2.8.0 with one changelog block covering D1–D21.

**⚠ Execution status (original, 2026-06-10 morning): BLOCKED by OneDrive file dehydration, not by complexity.** The decisive files are cloud-only placeholders right now and physically unreadable/uneditable from here — including the one file almost every change runs through (`pulse/api/app.py`) and the main view component. **Unblock (one minute, on the Mac): in Finder, right-click the `PROFIT_POOL_ENGINE` folder → OneDrive → "Always keep on this device", wait for the sync badge to finish.** Then the whole package below is executable and verifiable (typecheck + pytest) in one session. Also confirm `git status` works in that folder first — these are product-code deletions and want a branch + revert path (the audit found `.git` partially dehydrated too).

Currently blocked files (verified just now): `pulse/api/app.py`, `hooks/usePrism.ts`, `api/client.ts`, `components/dashboard/ProfitPoolAnalysis2.tsx` (+Backup), `components/dashboard/Trends2.tsx`, `components/dashboard/CategoryDetailPanel.tsx` (fluctuating), `package.json`.

### Step 1 — Backend detachment (app.py first — nothing else may be deleted before this)
- `pulse/api/app.py`: remove `from pulse.api.routes.delphi import router…` (≈line 74) and `app.include_router(delphi_router…)` (≈517); remove all `AllocationOptimizer` imports/uses (≈297, 445, 631, 1493 in the conflict copy's numbering — re-locate in the live file); remove `include_allocation`/`risk_aversion` from `SimulationRequest` (≈132), the allocation block in `/simulate`, the `"allocation"` key in its response, and the whole `/api/v1/optimize/allocation` endpoint; remove allocation from the DB-persist call and the cold-start restore path.
- D2 fixes in the same pass: `pulse/api/routes/analytics.py` — Sobol accessor `["path"][2030]["median"]`, real seed, deep-copy DB in trend mode (F-22); CVaR terminal-year + bootstrap SE + true top-5; tipping-point shape fix + conditional inflection.
- D1: `pulse/config_validation.py` — add PSD/symmetry validation for `force_correlation_matrix` (+ validate `within_force_overlap`, `category_weights`, `region_weights`); recalibrate `DEFAULT_FORCE_CORRELATIONS`/ρ in `pulse/config.py` to a valid-as-entered set; reject invalid PUT /config with a nearest-PSD suggestion.
### Step 2 — Module deletions (after Step 1 only)
- Delete `pulse/optimizer/` (entire), `pulse/elicitation/` (entire), `pulse/api/routes/delphi.py`.
- `pulse/database.py`: remove delphi DDL + helpers; write `scripts/migrate_drop_delphi.py` (archive `delphi_*` tables to JSON, then drop). `pulse/backup.py`, `pulse/audit/logger.py`: remove delphi references.
- `pulse/excel_bridge/writer.py`: remove Allocation sheet (≈lines 42-62 signature + Sheet 3). `pulse/api/export_pptx.py`: remove allocation from the strategic-implications slide.
### Step 3 — Frontend
- `app/dashboard/page.tsx`: remove `ProfitPoolExplorer` import + tab entry + render branch.
- Delete: `components/dashboard/ProfitPoolExplorer.tsx`, `lib/profitPoolData.ts`, `components/dashboard/{DelphiPanel,DelphiScoreCard,DelphiDistribution,AllocationChart,ScenarioSelectorPanel}.tsx` (orphans — re-verify importers once hydrated), `archive/InnovationExplorer.tsx` stays (already archived).
- `types/`: delete `delphi.ts`; remove delphi exports from `index.ts` and delphi fields from `trends.ts`; remove `AllocationRecommendation` + `allocation*` fields from `simulation.ts`; remove allocation context from `AIChatPanel.tsx`.
- `hooks/usePrism.ts`, `api/client.ts`: strip allocation/delphi calls + state (re-locate once hydrated).
- `components/dashboard/ProfitPoolAnalysis2.tsx`: D6 relabels (lens toggle + captions); D3 one-decimal sweep (`toFixed(1)` everywhere incl. tooltips/totals); ranges visible by default; headline joint band (from per-run percentiles) + seed-stability line replacing the R̂ badge.
- `components/dashboard/Trends2.tsx` + `CategoryDetailPanel.tsx`: D7 provenance chips ("AI suggestion" / "AI suggestion · expert-reviewed" from `ai_suggested`/`user_override`); ensure the admin Trend editor sets `user_override=true` on save (verify in `app.py` PUT /trends handler).
- `components/dashboard/SettingsModal.tsx`: remove the stale scalar "Attenuation (base)" field (F-27); add read-only per-force attenuation (6) + within-force overlap (6) with source tags; collapsible advanced read-only view of the two 6×6 matrices; "Modified (n)" chip data plumbed from config diff vs defaults.
### Step 4 — Tests, docs, claims
- Delete `tests/test_optimizer.py`; update `tests/test_api.py` (allocation/delphi endpoints → 404 assertions or removal); add endpoint tests asserting Sobol non-NaN (kept unexposed), CVaR terminal-year, tipping no-crash; add PSD-rejection test.
- `CLAUDE.md`: module table (Delphi/optimizer rows → removed; analytics rows → fixed-not-exposed), horizon text, two-audience doctrine restored (no € in tool), lens wording per D6.
- Bump MODEL_VERSION (engine contract changes: simulate response loses `allocation`) and add one changelog block covering D1-D10.
### Step 5 — Verify before deploy
- `npx tsc -p tsconfig.check.json --noEmit` clean; `pytest` green (after fixture updates); one local 10k run: integrity events must show **no** `correlation_pd_repair` on defaults (D1 success criterion); spot-check `/simulate` response shape against `types/simulation.ts`.
- Deploy to a Vercel preview before production; keep the previous production run row in the DB for diff.

---

## Part E — Execution record (2026-06-10)

**Status: EXECUTED** on branch `remediation-now-package` (off `main`), with the Profit-Pool-Explorer decision updated per owner: **remodeled to GP1-only and kept** (absolute revenue/pool figures allowed there), instead of deleted.

Verification at completion:
- `npx tsc -p tsconfig.check.json --noEmit` → **0 errors**.
- `pytest` (engine, excl. `test_scanner_routes.py`) → **118 passed**; 10 remaining failures are **pre-existing** auth-era tests (`test_api.py` asserts 4xx-without-401 on endpoints that have required admin auth since before this remediation; same assertions fail identically on `main`). `test_scanner_routes.py` is also pre-existing breakage (imports `_scan_source`, removed when external scanning was disabled).
- Default engine run (seed 42): **integrity_events = []** — the D1 success criterion; the PSD repair no longer fires.
- Golden pins regenerated for v3.6 defaults in the same commit, as the test's own policy requires; MODEL_VERSION → **2.7.0**.
- Frontend vitest could not launch in the audit sandbox (node/vitest ESM mismatch, pre-existing) — run `npx vitest run` locally once for the two frontend test files.

Manual follow-ups for the owner:
1. `git` in this repo was wedged by a stale `index.lock` dated **May 4** (removable only outside the sandbox): delete `.git/index.lock`, then review/commit the branch. Until then the changes exist as working-tree edits on `remediation-now-package`.
2. Tombstoned files (sync layer blocked deletion) — safe to delete manually: `pulse/optimizer/`, `pulse/elicitation/`, `pulse/api/routes/delphi.py`, `tests/test_optimizer.py`, `types/delphi.ts` (tombstone), `components/dashboard/{AllocationChart,DelphiPanel,DelphiScoreCard,DelphiDistribution,ScenarioSelectorPanel}.tsx`, and every `*-MacBook Air von Alexander.*` duplicate.
3. Run `python scripts/migrate_drop_delphi.py --dry-run` then live against prod Neon to archive+drop `delphi_*` tables.
4. Deploy to a **Vercel preview** first; re-run `scripts/run_50k_prod.py` so the persisted run reflects v3.6 defaults (expect small median changes; that is the D1 honesty correction, version-stamped).

---

## Part E.2 — Second-round execution record (2026-06-10, D12–D21 + Sobol rider)

**Status: EXECUTED** on branch `remediation/v3.7-d12-d21` off `main` (d279ce4). MODEL_VERSION → **2.8.0**; spec → CLAUDE.md v3.7.

**Reconciliation findings (D12).** The connected folder was ahead of the kickoff brief: the v3.6 remediation (`c0a650d`) and the platform line (Next 16 / React 19 / CI, `c9e60e8`) had already been unified into `main` via merge `4c7070c`, plus subsequent F-pass commits (F1 single-source math, F2 read-only online service, F3 authenticated reads, F4 app.py split, F6 dead endpoints, F10 dynamic imports, Maritime design, D7 ranges, chat removal). The broken `main-MacBook Air von Alexander` ref and the May-4 `index.lock` were already gone. Working tree was clean at `main` with the Jun-5/8 headline band present in `ProfitPoolAnalysis2.tsx` (F-26's four files re-verified readable; the stale-frontend-files concern was resolved by the merge). Backup tarball `_BACKUP_pre_v3.7_2026-06-10.tar.gz` + safety branch `backup/pre-v3.7-anchor` created before any change. Tombstones from Part E follow-up 2 were already physically deleted; follow-up 1 (index.lock) resolved.

**Executed this round:**
- **D14 + Sobol rider:** advanced-analytics suite deleted end-to-end — 4 engine modules, `routes/analytics.py` + mount, 4 client functions, `types/analytics.ts` (next + legacy dashboard), legacy-dashboard panels/hook state, 5 test files, SALib dependency.
- **D13:** `_scipy_compat.py` deleted; engine imports scipy directly and fails loudly; F2 guard re-implemented as a patchable environment probe (never a math fallback); engine-lite CI job removed; `numerics_backend` ("scipy 1.15.3 · numpy 2.2.6" at execution) recorded in results, persisted bundle meta, run_meta, Excel/Power BI metadata. Verified: no deployed/serverless path imports the engine outside the guarded `/simulate`.
- **D20:** post-D1 df re-test on the 99-trend base, 10k × seed 42 (`verification/v8_d20_tcopula_df_out.txt`): portfolio P10–P90 width moved −1.4% / +1.0% / +1.8% for df 4 / 30 / ∞ vs default — inert, <2%. t-copula deleted → Gaussian copula; `t_copula_df` removed from config, validator, ConfigUpdate, GET/PUT /config, SettingsModal, legacy SettingsPage; tolerant `ModelConfig.from_json` for old snapshots; golden pins regenerated for 2.8.0 in the same commit.
- **D21:** validator now covers every engine-consumed layer (force_correlation_matrix incl. symmetry + 6-level PSD; force_overlap_matrix zero-diagonal [0,0.45]; within_force_overlap [0,0.5]; region_weights; category_weights cross-checked vs category_names). Found & fixed in the sweep: **PUT /config crashed on every call** (validator read the v3.2-deleted `attenuation` attribute and omitted required `per_force_attenuation`); pydantic 400s were unserializable (raw ValueError in ctx → 500); zero-trend division warning guarded (F-25); quantile convention documented engine-wide; dead `include_sensitivity` flag, stale `'assumed'` description, duplicate response keys, and hardcoded "Model Version 2.5.0" in Excel/Power BI exports removed. Error-text items (50k→10k, df 4→8) were verified already fixed in v3.6.
- **D19:** `pulse/audit/input_drift.py` — per-trend fingerprint persisted in every bundle meta; runs diff vs the previous accepted run and emit `input_drift` integrity events (counts, probability/gp1 split, direction flips, adds/removes, per-force balance delta; warning severity on flips or >10 changes; zero-drift confirmation event otherwise). Integrity events + seed stability now persist and rehydrate; surfaced via a new `IntegrityChip` next to the run ribbon. Unit-tested (`tests/test_input_drift.py`) and verified end-to-end (two consecutive /simulate runs on SQLite produced the zero-drift event referencing the prior run id).
- **D3 residuals:** engine emits `totals.portfolio` — true joint portfolio percentiles (per-iteration category-weighted mean over raw samples); the headline band consumes it and labels itself "joint portfolio" (pre-2.8 runs fall back, labeled). `run_multichain` emits `seed_stability` (headline spread across chains; 10k × 3 chains measured 0.007pp); the run-details popover shows the seed-stability line and the R̂ row + glossary box are gone.
- **D16/D17:** exact owner wording placed — ceteris-paribus caption under the headline, "What the model holds constant" footer block, Excel READING NOTE; "structured-judgment overlap correction (v3.5, Apr-2026)" in SettingsModal source tags, Excel metadata, Power BI export.
- **Housekeeping:** empty `optimizer/`, `elicitation/`, `backtesting/` dirs, 3 `seed_trends.py.bak.*`, orphaned `data/mockData.ts`, `_tmp_slide_preview.jpg` deleted; OBSOLETE banners on the 7 legacy analytics/Delphi docs; `/simulate` now refreshes `run_meta` in state from the just-persisted run; CLAUDE.md rewritten as v3.7 against the verified codebase (route table, 13 components, 99-trend composition 52C/47E, 2026–2035 horizon).

**Verification at completion:**
- `pytest` (excl. pre-existing-broken `test_scanner_routes.py`): **71 passed, 0 failed** — including regenerated 2.8.0 golden pins, D19 drift tests, D13 backend-tag test, F2 guard test.
- Default-config engine run, 99 trends, 10k × 3 chains, seed 42: **integrity_events = []** (D1 criterion holds post-Gaussian); joint portfolio band 2035 = −4.6% (P10 −5.2% … P90 −3.8%); seed stability: headline spread 0.007pp across 3 chains.
- `/simulate` + `GET /simulation` response shape verified against `types/simulation.ts` (incl. `totals.portfolio`, `integrity_events`, `seed_stability`, `run_meta.numerics_backend`); PUT /config spectral gate verified live (ρ=0.25 → 400 with min-eigenvalue −0.518 message; ρ=0.35 dry-run validates).
- Frontend `tsc --noEmit` + vitest: recorded separately below once the sandbox npm install completes.

**Deploy status (2026-06-10, this session):**
- **Vercel preview deployed and verified healthy**: `dpl_6q8BF4khG6mrdCNnYjUp9by1xD2w` (CLI upload of the exact v3.7 tree; build READY; nodejs×3 + python×1 lambdas). `/api/v1/health` on the preview returns `{status: ok, trend_count: 99, has_simulation: true, latest_run_id: 91}` — the v3.7 service imports cleanly without scipy/analytics and reads prod Neon read-only. **Production deployment untouched** (still d279ce4); prod run row #91 (2.7.0) intact for diff.
- **GitHub push blocked from the sandbox**: the `.env.deploy` PAT lacks the `workflow` scope and v3.7 modifies `.github/workflows/ci.yml` (engine-lite removal). All work is committed locally — branch `remediation/v3.7-d12-d21` and `main` both at the v3.7 merge.

**Owner runbook to production:**
1. Push from the Mac (normal credentials) or add the `workflow` scope to the PAT, then: `git push origin remediation/v3.7-d12-d21 main`.
2. Eyeball the preview (Vercel login): headline band + D16 caption, integrity chip (appears once a run carries events), Settings copula section (no df field), run popover (seed stability + numerics backend). Note: the "joint portfolio" band label appears only after step 3 persists a 2.8.0 run.
3. After the production deploy: `python3 scripts/run_50k_prod.py` — persists the first 2.8.0 run (Gaussian copula; expect small median changes vs the 2.7.0 row — version-stamped honesty correction; the previous row stays for diff). The run also writes the first input-drift fingerprint baseline.
4. Optional: `scripts/migrate_drop_delphi.py` against prod if not yet done (Part E follow-up 3).

---

## Part F addendum — owner rulings of 2026-07-07 (post-handover-review)

| ID | Decision | Ruling & execution |
|----|----------|--------------------|
| O1 | Delphi legacy: delete everything delphi-related | **Approved.** The delphi-era `trends` columns (`scorer_count`, `score_variance`, `debiasing_applied`) are removed from the dataclass, DDL, save/load, API serialization, revert logic and TS types; `scripts/migrate_drop_delphi.py` extended to archive-then-drop the columns alongside the `delphi_*` tables — run once per database (executed against the local DB 2026-07-07; prod run on the operator checklist, AFTER deploying the new code). Non-delphi legacy (`users.password_*`) explicitly NOT in scope — stays on the DX migration backlog. **Execution incident, disclosed:** the first "local" invocation silently resolved to prod Neon (`.env` `DATABASE_URL` outranks `PRISM_DB_PATH`) and dropped the three columns there ahead of sequence. Prod was restored to its exact prior state within minutes (`ADD COLUMN … DEFAULT` — verified 99/99 rows at the default values they had always held; the columns never contained non-default data). The script now refuses Postgres without an explicit `--postgres` acknowledgement (same loud-refusal philosophy as H1). |
| O2 | Consumer-journey AI content: "no pending review, just accept them" | **Approved.** The AI-derived journey tiles (already folded into the 2026-06-29 seed reformulation — the seed carries 300 strategist-attributed tiles) and the 260 tile-derived exposure scores are bulk-accepted as working values. UI copy now cites the acceptance (O2) instead of "pending strategist review"; the ✨/pending-review provenance machinery remains live for FUTURE AI suggestions (D7 unchanged). Honest-labelling note: this is an owner bulk-acceptance, not an item-level strategist review — recorded as such. |
| O3 | Journey exposure scores: "the 260 scores should be deleted. I don't need them." | **Approved.** The entire quantitative journey layer is deleted before ever being activated in prod: `journey_exposure` field + persistence, the 260-score seed (`pulse/seed_journey_exposure.py`) and backfill script, the engine's `journey_decomposition` + the stage taxonomy in `pulse/config.py`, the drift-fingerprint key, API/TS contract keys, and the per-stage exposure UI. The qualitative overlay (tiles, Strategist Reads, evidence cards, `journey_content` store) is unaffected. Contract-symmetric with the June addition (ruled additive): no MODEL_VERSION bump, golden pins untouched — locked by negative contract tests. Local DB migrated (260 rows archived to `data/archive/`, gitignored); prod via `scripts/migrate_drop_legacy.py --postgres` after deploy. |
| O4 | Remaining legacy schema: "do that diligently" | **Approved.** `scripts/migrate_drop_legacy.py` (archive-first, `--postgres`-gated) drops the engine-side `users` table (pre-Clerk auth; zero code references — identity is Clerk, roles live in the Next-managed `user_roles` table; 3 legacy rows archived locally incl. their obsolete password hashes — archive directory gitignored), the `scanned_trends` table (scanner deleted with R2; 0 rows), and the `simulation_runs.allocation_recommendation` column (optimizer, D4; NULL on every row — parameter and DDL removed from code). `ai_suggestions` and `triggers` were checked and left: they are referenced by live code paths. Local DB migrated; prod on the operator checklist. |

---

## Part G — owner ruling of 2026-07-10 (VC epicentre attribution)

| ID | Decision | Ruling & execution |
|----|----------|--------------------|
| O5 | VC attribution must be revised for the epicentre input grammar (July 2026 Trends-editor redesign made the value chain a single epicentre stage per trend; the engine still consumed the 8-step profile expanded from the slider's 5/3/1 serialization kernel × `vc_weights`) | **Approved & executed (MODEL_VERSION 2.9.0).** The VC lens is now a **categorical epicentre partition**, structurally parallel to the force lens: each trend's relevance (\|normalized_score\| × cat_exposure/5) is assigned wholly to `pulse.config.vc_epicentre_step_of(vc_exposure)` — the engine-side twin of the frontend's `epicentreOf`, pinned to the same fixture table on both sides (`tests/test_vc_epicentre.py` ↔ `tests/frontend/vcEpicentre.test.ts`), so legacy arbitrary profiles collapse identically everywhere and **no data migration is needed**. Rationale: the expert input carries one bit of VC information (the stage); smearing it through a UI serialization constant rendered pseudo-measured cross-step shares (the D3/D17/F-19 failure mode). Executed with it: (a) **`vc_weights` deleted end-to-end** (ModelConfig + defaults, validator, ConfigUpdate, GET/PUT /config, SettingsModal grid, TS types — inert at equal defaults, meaningless over categorical votes; `from_json` tolerates old snapshots; the `config_snapshots.vc_weights` column stays as an inert never-written legacy column on the DX backlog); (b) **honesty events**: unscored trends emit a `vc_epicentre_coverage` warning (pre-2.9 they silently vanished from the lens), and a category with zero epicentre-scored contributors emits `vc_attribution_fallback` for its uniform 1/8 spread (degenerate guard only — never fires on the 99-trend base); (c) **drift telemetry** fingerprints the derived stage, not the raw profile — an epicentre flip is drift, a representation rewrite is not; pre-2.9 dict-format fingerprints are collapsed before diffing so the first 2.9.0 run reports no false drift; (d) **labels**: lens reads "Value chain epicentre attribution" with a no-propagation caption (D16 wording family), About-footer + persisted `meta.vc_attribution_basis="epicentre"` (older runs render "profile-weighted (pre-2.9 run)"), Excel metadata carries a VC ATTRIBUTION note, Trends-editor copy no longer claims the engine consumes the 8-step expansion; (e) the duplicated terminal-year `vc_decomposition` block now reuses the same shares (single source). **Shift-matrix numbers are untouched** — `vc_exposure` never fed the shift math; golden shift/portfolio pins are deliberately NOT regenerated (locked by the 2.8.1-valued pins passing on 2.9.0); only `decompositions.vc`/`vc_decomposition` values move. New structural locks: VC reconciliation identity, categorical-partition leak test, coverage-event test. Gates at execution: pytest 108, vitest 59 (incl. parity suite), typecheck clean, eslint 0 errors, single-source guard OK. Operator: re-run `scripts/run_50k_prod.py` after deploy so the persisted run matches 2.9.0. |


---

## Part H — owner rulings of 2026-09-03 / 2026-09-10 (September 2026 trend-base review, release 2.11.0 and its go-live)

Context: the senior-partner review of the trend base (2/3 September 2026; deliverables in the owner's working-files folder, v3) reduced the 99-trend base to 51 drivers, re-scored every parameter and introduced one uncertainty score per driver. The owner then asked for the implementation ("Implement it in the code. Work very diligently. You are the senior tech lead for PRISM and need to ensure 100% accurate implementation.") and took the decisions below in the chat on 2026-09-10 (plan v1 and v2 in the working-files folder). MODEL_VERSION bumped to **2.11.0**; golden pins regenerated in the same commit; **run the 50k CLI after deploying** and after the base replacement.

| ID | Decision | Ruling & execution |
|----|----------|--------------------|
| O6 | Cell-level gross-profit weights. "The relevant part of the P&L that really sits in the category x region combination is used and multiplied." The euro P&L never enters PRISM: **shares only**; the separate region and category dials are **replaced by one 12 x 4 grid** (marginals read-only); the actual shares arrive as a **file passed to the CLI** (kept outside git); until then **equal 1/48** for every cell (regions 25 percent each, categories equal, "later we will add actuals"). | **Approved & executed.** `ModelConfig.cell_weights` (12 x 4 shares, sum 1, validated for completeness, non-negativity and sum within ±0.01; zero rows allowed and reported by a `cell_weight_zero_row` integrity event); `region_weights` / `category_weights` are derived marginals (`from_json` drops them when a matrix is present; a legacy vector still builds the separable outer product, which is how the 2.10.0 numbers are reproduced). Engine: category shift = share-weighted mean of the category's row, portfolio = Σ share x cell, region-lens shares from the cell matrix; the result and the persisted meta carry `cell_weights_used`, `category_weights_used`, `region_weights_used` (column sums) and `cell_weights_source`. `run_50k_prod.py --cell-weights FILE` (JSON with `source`, `basis` = `gp1_share` or `gp1_absolute`, `cells[category][region]`; absolute figures are normalised to shares before the engine is built and never persisted; exit 6 on rejection). `PUT /api/v1/config` accepts `cell_weights` + `cell_weights_source` only and returns 400 for `region_weights` / `category_weights`. Config sheet: one 12 x 4 grid with row and column marginals, the Σ badge, a reset-to-equal button and the source label. About footer: cell-weight block, region and category shares, source label; drill-down: each category's own row shares. Excel: a "Cell Weights" sheet. Lock: `tests/test_cell_weights.py` — the separable Group-split matrix with the 2.10.0 calibration reproduces the 2.10.0 fixture run to 1e-12 (category cells, portfolio band, velocity bands with jitter on, region lens), plus the roll-up algebra and the contract. |
| O7 | One uncertainty score per driver ("There should only be one uncertainty, not several like magnitude and timing"). | **Approved & executed.** `Trend.uncertainty` 0-5 (optional) in the dataclass, both database tables (trends, trend_score_proposals; `ALTER TABLE ... ADD COLUMN`), the API (create, update, proposals with a median aggregate), the drift fingerprint (key "u"; pre-2.11 fingerprints without it diff cleanly), the Trends editor (expert form, admin edit panel, Review & Endorse with AI / expert / manual choice), the AI-baseline snapshot and the backfill script. Engine: Beta concentration κ = 24/16/10/6/4/3 by score with the mean p/6 unchanged (the published score → prior table stands; floors 0.5), and per-driver peak-year jitter width 0/1/1/2/3/4 years drawn by an inverse-CDF sampler that is bit-identical to the 2.10.0 `rng.choice` stream when every trend is unscored. `config.peak_year_jitter = 0` keeps its 2.10.0 meaning as the engine-wide off switch (golden fixture, diagnostics). An unscored trend behaves exactly as in 2.10.0 (κ 6, global jitter). Lock: `tests/test_uncertainty.py`. |
| O8 | Türkiye belongs to Henkel's High Growth region. | **Applied in the review** (regional exposures of the affected drivers) and carried into the seed; no engine change. |
| O9 | Category exposure and regional exposure are independent, mechanism-only dimensions; **no presence index** — presence enters through the cell P&L (O6). | **Approved & executed.** The interim presence index of review v3 was withdrawn and the exposures restored to the mechanism-only v2 values (challenge log, ALL entry); the seed carries the reviewed exposures unchanged; the editor copy and `docs/SEED_DATA_README.md` state the rule. |
| O10 | Replace the 99-trend base with the 51-driver core set. Provenance in the editor: **no label** (neither AI-suggested nor expert-reviewed chips). Replacement by an **archive-first script run by the owner**, SQLite first, then Neon. | **Approved & executed.** `pulse/seed_trends.py` and `data/trendCodeMap.ts` are now GENERATED by `scripts/generate_seed_from_core_set.py` from `data/trend_base_2026-09/core_set_51_v3.json` (the reviewed set) and `verdicts_99_v1.json` (the disposition of every base code); `--check` verifies the committed outputs and CI runs it. Identity rules: kept drivers keep their ids (37); new drivers take the number of their review code (12); the two force moves get an id in the new force — T-06 retail media → `customer_r13` / K-13 (Technology → Customer), X-06 growth markets → `consumer_r37` / C-37 (Competitive → Consumer) — with the old codes pointing at the new ones. Every driver is seeded with `ai_suggested=False`, `user_override=False` (the editor's provenance chip renders only for AI-suggested or admin-edited trends) and its baseline snapshot incl. the uncertainty score. The code map's `RETIRED_CODES` (65 entries: 3 pre-2.11, 62 from this review) carry `mergedInto` (37 codes: merges, substitutions, force moves), `residueIn` (18 deleted codes whose fact lives on inside a driver) or the review's one-line reason (7 deleted outright); journey evidence cards resolve merged codes to the live driver ("cited as K-08, carried inside K-13"), count each live driver once per tile, and keep deleted codes as muted retired cards. `scripts/replace_trend_base.py` archives trends, exposures, sources and proposals to `data/archive/` before anything changes, deletes the retired ids, writes the 51, **re-inserts the expert proposals of the kept ids** (see F-29: on Postgres `save_trends` cascades them away), verifies (51 rows, 12/4/8 exposure rows and at least one source per driver, every driver scored), writes an audit-log entry, refuses Postgres without `--postgres`, and reports only with `--dry-run`. `EXPECTED_TREND_COUNT` 99 → 51; the UI counts are live. Composition: Consumer 20, Government 10, Customer 7, Technology 6, Competitive 4, Environmental 4; 33 Contraction / 18 Expansion. The first 2.11.0 run reports the replacement as a critical input-drift event by design (D19). Locks: `tests/test_trend_base_2026_09.py`, `tests/frontend/trendCodeMap.test.ts`. |
| O11 | Recalibrate everything that depends on the trend population; mechanism adjustments **re-judged per cell**. | **Approved & executed (v3.11).** `scripts/compute_attenuation_v3_11.py` reruns the v3.1/v3.5 method on the 51 drivers (J0 0.4265 over 1,275 pairs) with per-cell mechanism adjustments re-judged and documented (record `data/attenuation_calibration_v3_11.json`, workbook via `build_attenuation_xlsx.py v3_11`, methodology sections 12-18). New defaults in `pulse/config.py` — per-force attenuation Consumer 0.487, Customer 0.418, Technology 0.415, Government 0.446, Environmental 0.379, Competitive 0.480 (trend-weighted mean 0.4520, v3.5: 0.4523); within-force overlap Consumer 0.100, Customer 0.223, Technology 0.373, Government 0.126, Environmental 0.389, Competitive 0.100; the cross-force matrix; `DEFAULT_ATTENUATION_SOURCE = "calibrated_v3.11_september2026"` (validator, TS type, Config sheet and Excel tags follow). `DEFAULT_FORCE_CORRELATIONS` re-checked on the 51 mix: minimum eigenvalue +0.414 (99 mix: +0.14), kept unchanged. In transferring the calibration, F-28 surfaced: the 2.10.0 config held the v3.5 attenuation next to v3.1 within-force overlaps and a hand-edited matrix, all labelled v3.5 — since 2.11.0 all three layers come from one generated record and `tests/test_calibration_v3_11.py` locks defaults to record, record to script, the eff_att identity and the copula margin. The 2.10.0 regression in `tests/test_cell_weights.py` pins the 2.10.0 calibration explicitly. |
| O12 | F-29: expert proposals must survive an edit. Owner instruction, 2026-09-10 after the go-live: "it is crucial that changes to drivers are saved and expert proposals remain visible". | **Approved & executed.** `pulse.database.save_trends` no longer deletes the trend row before re-inserting it: the row is upserted (`INSERT INTO trends (...) VALUES (...) ON CONFLICT (id) DO UPDATE SET <every written column> = EXCLUDED.<column>, updated_at = CURRENT_TIMESTAMP`), so `trend_score_proposals.trend_id REFERENCES trends(id) ON DELETE CASCADE` cannot fire on a write. Every admin write path is affected and intended: `PUT /api/v1/trends/{id}` (the Review & Endorse flow), `POST /trends`, `trends/sync`, `full-reseed`, `revert-to-seed`, `POST /seed`, `scripts/replace_trend_base.py` and the state-layer backfill. The four derived child tables (category, VC and regional exposure, sources) keep their delete-then-insert, because a save replaces them wholesale from the incoming object. A genuine deletion (`DELETE /api/v1/trends/{id}`, the retired ids inside the replacement script) still cascades the proposals away, which is the intended semantic: a proposal belongs to its driver. Side effect, deliberate: `created_at` survives an edit instead of being reset. The replacement script keeps its snapshot-and-restore step as a safety net for a database last written by pre-2.11.0 code, and its report now reads "N snapshot, 0 re-inserted" on a healthy base. Lock: `tests/test_trend_persistence.py`, eight tests run with `PRAGMA foreign_keys = ON` so the SQLite path behaves exactly like Neon: proposals survive a single and repeated edits, every scoreable field is persisted and read back, exposures and sources are still fully replaced, a create still inserts, `created_at` survives, a real deletion still cascades, and end to end a `PUT /api/v1/trends/{id}` is persisted while `GET /api/v1/trends/{id}/proposals` still lists both experts. The suite was verified against the old code first: three of the eight fail without the fix, including the API-level one. pytest 197 passed. |

**Release cut (owner):** one release, 2.11.0, one pin regeneration, one 50k run.

**Adversarial review (2026-09-10, independent agent on the working tree, 18 findings, all closed before hand-back):** the substantive one was a latent 2.10.0 defect that the O7 widths made reachable — a jittered peak at or before the onset fell into the schedule's "peak ≤ onset ⇒ ramp to the horizon" branch, so the earliest draw became the latest arrival (four trends of the 99 base at ±1; more with ±2 to ±4). Fixed by clamping the jittered peak at onset + 1 (`_peak_schedule_table`); terminal-year numbers are unchanged, intermediate years of the 99 base move by at most 0.13 pp, the 2.10.0 reproduction lock holds. Also closed: the zero-row / fallback cell-weight events were listed twice on the multichain path (deduped by type); the replacement script now refuses a second run on a database already on the 51 base (exit 7, `--force` overrides after reporting the expert-reviewed rows it would reset), applies no schema change on a dry run, and anchors its archive on the repository; the reseed and sync endpoints refuse (409) to act as a base replacement when the id sets differ by more than ten; an explicit `null` clears the uncertainty score through the API (an absent field leaves it unchanged); an empty `cell_weights_source` is rejected and never rendered as the placeholder text; booleans are not accepted as cell values; `copy_with` raises when the matrix and the legacy vectors are passed together; the unscored Beta prior keeps the 2.10.0 formula verbatim; the journey evidence cards render one card per resolved live driver naming every merged citation; the Config sheet's reset writes the label the backend serves. Two pre-existing defects are recorded rather than fixed: F-28 (resolved by O11) and F-29 (open: the proposals cascade on Postgres).

**Verification at completion (2026-09-10, cloud workspace):** pytest 189 passed (incl. the regenerated 2.11.0 golden pins, the 2.10.0 reproduction locks, uncertainty, trend base, calibration); vitest 71 passed (incl. the code-map and cell-weights suites); `tsc --noEmit` clean; eslint 0 errors; the shift-matrix single-source guard OK; `generate_seed_from_core_set.py --check` OK; the replacement script dry-run and live run on a copy of the local SQLite base (99 → 51, archive written, proposals of kept ids restored). Acceptance runs and the delta table: see the release summary. Owner steps: review the working tree, commit on a branch, deploy a Vercel preview, run `scripts/replace_trend_base.py` on the local SQLite then on Neon with `--postgres`, then `scripts/run_50k_prod.py` (with `--cell-weights FILE` once the P&L shares exist).

**Consumer Journey re-basing (2026-09-10, under O10; owner request "ensure that the Consumer Journey view is reviewed and adjusted again"):** the 300 authored tiles and the 21 stage contexts of `data/consumerJourney.ts` were re-based onto the 51-driver base. Rules applied per tile: every citation now names a live code only (merged and substituted codes are attributed to the absorbing driver with its live name; residue codes only where the target still carries the tile's mechanism); a driver that left the model is no longer claimed, the affected sentences read as the strategist's judgment and one sentence says that the trend left the model in the September 2026 review (watch list); a driver whose category exposure is zero on the tile's journey (hair-only drivers such as C-03, C-04, C-07, G-03, K-04, K-07 on laundry tiles; laundry-only drivers such as T-08, C-28 on hair tiles) is not cited; the tile's side (benefiting or declining) is the strategist's and is never flipped, a driver working against it is named as a headwind or tailwind working through the moment; no new numbers, sources or company facts; the argument, brand names and length (within about 15 percent) are kept; no em-dashes. Result: 153 tiles cite a different code set than in June; 233 tiles follow the mapping mechanically (codes, live names and the affected sentences), 48 carry a documented judgment call, 19 tiles have no modelled driver left (their driver note says which code left the base and why) and render an honest empty driving-trends step. Provenance date 2026-09 on every tile (author stays `strategist`, evidence grades unchanged); `JOURNEY_CONTENT_VERSION` 2026-09-10. The profit-pool impact chip (graded June 2026 on the 99 base, tercile-balanced) was re-graded by a tercile-delta rule: score = max over the tile's cited live drivers of |normalized_score| x probability/5 x gp1_pct_affected, times the tile intensity; tercile rank over the 300 tiles on each base; new grade = June grade shifted by the tile's tercile change between the 99 base (old codes) and the 51 base (new codes), clamped; no live driver = Low; direction unchanged. The formula reproduces the June grades at 79 percent on the 99 base, which is why the June judgment is shifted rather than replaced; 141 grades moved (68 up, 73 down; now High 93 / Med 116 / Low 91), most of them because the review cut T-01, T-02, K-04, G-02 and G-05 and raised T-03, G-04, C-06 and C-29. The re-basing record (per-tile change log, the merged agent outputs, the re-grade script and the generator) is in the owner's working-files folder (`2026-09-10_journey_rebasing/`); the seed is the server copy's fallback, so the same content is written as a new `journey_content` row at go-live (the previous rows stay for rollback).

**Go-live record (2026-09-10, owner instruction "run a simulation and put it into live"):** branch `release/2.11.0`, commit `75bcfe4` (61 files, 14,440 insertions), pushed by the owner and fast-forwarded into `main`. Vercel preview `dpl_FKiFP3bRCcVHHynA4NTfu4FnUSHw` verified first (build READY, `/api/v1/health` version 2.11.0, Neon reachable, `/api/v1/simulation` 401 and the pages 307 as designed), then production `dpl_BfGbWZdD9PCpDBV1EkeYwFQ9LqJJ`. Sequence on Neon afterwards: `replace_trend_base.py --postgres` (archive `data/archive/trend_base_postgres_20260910_195956.json`, 99 trends with 1,188 category, 792 value-chain, 396 regional exposure and 399 source rows plus 8 expert proposals; 62 retired ids deleted, the 51 drivers written, the 4 proposals on kept ids re-inserted after the Postgres cascade, F-29; verification 51 drivers, Consumer 20 / Government 10 / Customer 7 / Technology 6 / Competitive 4 / Environmental 4, 33 contraction / 18 expansion, 612 / 408 / 204 exposure rows, 235 sources, every driver scored, no provenance flag; audit entry `trend_base_replaced`). The write phase was interrupted once by the 180-second limit of the remote shell used for the go-live and rolled back cleanly (the archive and the delete were already committed), then completed in slices through the same `save_trends` path (one-off resume script in the owner's working-files folder); both operational lessons are now in DEPLOY.md. Then `run_50k_prod.py`: 3 × 50,000 iterations, 29.3 s engine time, correlation pre-flight min eigenvalue +0.4135 on the 51 mix, MC standard error 0.0020 pp worst category, seed stability 0.0028 pp, persisted as `simulation_runs` id 98 with the full 2.11.0 contract, QA workbook written. **Portfolio 2035 median -4.4 percent (P10 -4.8, P90 -3.8)**, matching the acceptance run R5 (-4.34 [-4.81, -3.79]) to the tenth; the input drift of the base replacement is recorded on the run as designed (36 score changes, 1 direction flip, 37 structural changes, 14 added, 62 removed). The re-based Consumer Journey content was written as `journey_content` row 3 (300 tiles, 44 live codes; rows 1 and 2 kept for rollback) and read back identical. Smoke test on https://prism-hcb.vercel.app after the owner's redeploy: version 2.11.0, `trend_count` 51, `latest_run_id` 98. The local SQLite base was moved to the 51 drivers the same day (archive in `data/archive/`).

**Hygiene close-out (2026-09-11, owner instruction "finalize the cleanup" and "fix all of these issues now"):** the legacy cleanup migration finally ran against Neon, months after it had been run locally. `scripts/migrate_drop_legacy.py` was first extended to cover the two tables the v3.2 deletions left behind (`backtest_results`, `causal_edges`) alongside the three it already carried (`trend_journey_exposure`, `users`, `scanned_trends`) and the dead `simulation_runs.allocation_recommendation` column; every table was archived to `data/archive/` before it was dropped, and only `users` held a row. Dropping `users` also removed the `password_hash` / `password_salt` pair that the handover backlog carried separately. One latent defect surfaced while preparing it: `get_db_stats` still counted `users`, and because Postgres aborts a transaction on a failed statement, the first missing table would have reported every later table as zero; the list is corrected and the handler now rolls back. The react-compiler advisory backlog was burned down in the same pass: all 19 warnings are gone (set-state-in-effect replaced by render-phase derivation, `useSyncExternalStore` for the two sessionStorage gates, promise-chain continuations in `usePrism` so the mount effect writes no state synchronously, and the `loadAllRef` indirection deleted because the circular dependency it worked around did not exist), the rules stay at `warn` as a ratchet, and the three retired one-line tombstones in `lib/` are deleted. Gates after the sweep: pytest 197, vitest 71, `tsc` clean on both configs, eslint 0 warnings 0 errors. Behaviour is unchanged except for one unreachable edge recorded in the working files: the admin-only invariant on the Trends review mode is now derived during render, so an `isAdmin` true-false-true round trip would leave the user on "review" rather than "list"; `isAdmin` only ever transitions once.

**Cell-weight sensitivity (2026-09-11, owner question):** a scenario run on estimated HCB gross-profit shares, not persisted and not a production run, puts the terminal-year portfolio at **-5.6 percent against the -4.35 percent the live equal-1/48 run shows**, with a plausible range of -5.2 to -5.6 across weighting variants. Decomposed: the estimated category mix alone accounts for -0.19 pp, the estimated region mix alone for -0.95 pp. The driver is regional concentration, not the category split: the placeholder puts 25 percent of the pool in each region while HCB's gross profit is roughly half European, and Europe is the most exposed region (-7.1 percent against -3.1 percent for High Growth) because the regulatory stack and the energy-cost disadvantage land there. This is an estimate with graded assumptions, so it is not a correction to the published number, but it sizes the open item: loading the real cell weights is worth about a point and a quarter of headline exposure and changes the regional story materially. Working files: `_NOT_FOR_HANDOVER/working-files-2026-09/2026-09-11_hcb_weights_scenario/`.
